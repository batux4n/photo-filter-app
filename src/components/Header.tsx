import React from "react";

// Header component: Displays the main header of the application
const Header: React.FC = () => {
  return (
    <header>
      <h1>Photo Filter Application</h1> {/* Main title of the application */}
    </header>
  );
};

export default Header;
